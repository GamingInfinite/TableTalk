import './App.svelte.css.proxy.js';
/* src/App.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal
} from "../snowpack/pkg/svelte/internal.js";

import { Deck } from "./util.js";

function create_fragment(ctx) {
	let div2;

	return {
		c() {
			div2 = element("div");

			div2.innerHTML = `<div class="title svelte-em42kk">TableTalk</div> 
  <div class="buttons"><button class="playButton svelte-em42kk">Play</button></div>`;

			attr(div2, "class", "titleScreen svelte-em42kk");
		},
		m(target, anchor) {
			insert(target, div2, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div2);
		}
	};
}

function instance($$self) {
	var testDeck = new Deck(true);
	testDeck.shuffle();
	return [];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default App;